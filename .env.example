ANTHROPIC_API_KEY=
# OpenAI API key (optional). Leave blank to disable the OpenAI provider.
# When blank, Jarvis uses only Claude for AI completions.
OPENAI_API_KEY=
# Google Gemini API key (optional). Leave blank to disable the Gemini provider.
# When blank, Jarvis routes to the next available provider.
GOOGLE_API_KEY=
AI_MODEL=claude-sonnet-4-6
AI_MAX_TOKENS=4096
# AI reasoning is off by default; Jarvis runs fully rule-based with no AI calls.
AI_REASONING_ENABLED=false
DATABASE_PATH=data/jarvis.db
LOG_LEVEL=INFO
APPROVAL_TIMEOUT_SECONDS=60
DEBUG=false

# Voice output (Phase 41) - off by default. VOICE_PROVIDER=fake only
# constructs a silent, audio-free test provider; there is no real
# text-to-speech engine yet. Accepted values: VOICE_SPEAK_MODE=off/all,
# VOICE_PROVIDER=none/fake.
VOICE_ENABLED=false
VOICE_SPEAK_MODE=off
VOICE_PROVIDER=none

# Voice input (Phase 41) - off by default. VOICE_INPUT_PROVIDER=fake only
# constructs a silent, microphone-free test provider; there is no real
# speech-to-text engine or microphone access yet. Accepted values:
# VOICE_INPUT_PROVIDER=none/fake.
VOICE_INPUT_ENABLED=false
VOICE_INPUT_PROVIDER=none

# Markdown "3D brain" integration - off by default. Jarvis reads (and,
# after explicit approval only, creates/updates) plain Markdown notes under
# BRAIN_PATH. BRAIN_PATH is the brain's root directory; Windows paths are
# accepted (forward slashes recommended). BRAIN_FOLDERS is the comma-
# separated list of included Markdown subfolders directly under that root -
# only these folders are ever scanned; the visualizer (apps/) and every
# hidden/.git/node_modules folder are always excluded. The limits bound a
# single search, a single note read, and the excerpts handed to advisory
# AI reasoning. Jarvis never deletes a note.
BRAIN_ENABLED=false
BRAIN_PATH=
BRAIN_FOLDERS=context,decisions,references,audits,brainstorms
BRAIN_MAX_FILE_BYTES=262144
BRAIN_SEARCH_LIMIT=25
BRAIN_AI_CONTEXT_CHARS=4000